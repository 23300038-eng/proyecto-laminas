<?php
$esAdmin = !empty($_SESSION['bit_administrador'] ?? false);
$p = $permisos_modulo ?? ['agregar'=>$esAdmin,'editar'=>$esAdmin,'consulta'=>$esAdmin,'eliminar'=>$esAdmin,'detalle'=>$esAdmin];
?>
<nav class="breadcrumbs">
    <a href="<?= $this->url('dashboard', ['action'=>'index']) ?>">Dashboard</a>
    <span>/</span>
    <span>Seguridad</span>
    <span>/</span>
    <span>Permisos-Perfil</span>
</nav>

<div class="page-header">
    <h2>Gestión de Permisos por Perfil</h2>
    <?php if ($p['agregar']): ?>
        <a href="<?= $this->url('security', ['action'=>'permiso-perfil-add']) ?>" class="btn btn-primary">+ Asignar Permiso</a>
    <?php endif; ?>
</div>

<?php if (!empty($error)): ?>
    <div class="alert alert-error"><?= htmlspecialchars($error) ?></div>
<?php endif; ?>

<div class="table-container">
    <table class="table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Perfil</th>
                <th>Módulo</th>
                <th style="text-align:center">Agregar</th>
                <th style="text-align:center">Editar</th>
                <th style="text-align:center">Consultar</th>
                <th style="text-align:center">Eliminar</th>
                <th style="text-align:center">Detalle</th>
                <?php if ($p['detalle'] || $p['editar'] || $p['eliminar']): ?>
                    <th style="text-align:center">Acciones</th>
                <?php endif; ?>
            </tr>
        </thead>
        <tbody>
            <?php if (!empty($permisos)): ?>
                <?php foreach ($permisos as $permiso): ?>
                    <tr>
                        <td><strong><?= htmlspecialchars($permiso['id']) ?></strong></td>
                        <td><?= htmlspecialchars($permiso['str_nombre_perfil'] ?? '-') ?></td>
                        <td><?= htmlspecialchars($permiso['str_nombre_modulo'] ?? '-') ?></td>
                        <td style="text-align:center"><?= $permiso['bit_agregar'] ? '<span class="badge badge-success">✓</span>' : '<span class="badge badge-secondary">✗</span>' ?></td>
                        <td style="text-align:center"><?= $permiso['bit_editar'] ? '<span class="badge badge-success">✓</span>' : '<span class="badge badge-secondary">✗</span>' ?></td>
                        <td style="text-align:center"><?= $permiso['bit_consulta'] ? '<span class="badge badge-success">✓</span>' : '<span class="badge badge-secondary">✗</span>' ?></td>
                        <td style="text-align:center"><?= $permiso['bit_eliminar'] ? '<span class="badge badge-success">✓</span>' : '<span class="badge badge-secondary">✗</span>' ?></td>
                        <td style="text-align:center"><?= $permiso['bit_detalle'] ? '<span class="badge badge-success">✓</span>' : '<span class="badge badge-secondary">✗</span>' ?></td>
                        <?php if ($p['detalle'] || $p['editar'] || $p['eliminar']): ?>
                            <td style="text-align:center">
                                <div class="btn-group">
                                    <?php if ($p['detalle']): ?>
                                        <a href="<?= $this->url('security', ['action'=>'permiso-perfil-detalle','id'=>$permiso['id']]) ?>" class="btn btn-sm btn-secondary">Ver</a>
                                    <?php endif; ?>
                                    <?php if ($p['editar']): ?>
                                        <a href="<?= $this->url('security', ['action'=>'permiso-perfil-edit','id'=>$permiso['id']]) ?>" class="btn btn-sm btn-secondary">Editar</a>
                                    <?php endif; ?>
                                    <?php if ($p['eliminar']): ?>
                                        <a href="<?= $this->url('security', ['action'=>'permiso-perfil-delete','id'=>$permiso['id']]) ?>" class="btn btn-sm btn-danger" onclick="return confirm('¿Eliminar este permiso?')">Eliminar</a>
                                    <?php endif; ?>
                                </div>
                            </td>
                        <?php endif; ?>
                    </tr>
                <?php endforeach; ?>
            <?php else: ?>
                <tr><td colspan="9" class="empty-state"><p>No hay permisos asignados aún.</p></td></tr>
            <?php endif; ?>
        </tbody>
    </table>
</div>

<?php if (!empty($pages) && $pages > 1): ?>
    <div class="pagination">
        <?php if ($page > 1): ?>
            <a href="?page=1">« Primera</a>
            <a href="?page=<?= $page - 1 ?>">‹ Anterior</a>
        <?php endif; ?>
        <?php for ($i = 1; $i <= $pages; $i++): ?>
            <?php if ($i === $page): ?>
                <span class="pagination-current"><?= $i ?></span>
            <?php else: ?>
                <a href="?page=<?= $i ?>"><?= $i ?></a>
            <?php endif; ?>
        <?php endfor; ?>
        <?php if ($page < $pages): ?>
            <a href="?page=<?= $page + 1 ?>">Siguiente ›</a>
            <a href="?page=<?= $pages ?>">Última »</a>
        <?php endif; ?>
    </div>
<?php endif; ?>
