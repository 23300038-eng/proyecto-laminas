<?php if (!empty($breadcrumbs)): ?><ul class="breadcrumbs"><?php foreach ($breadcrumbs as $breadcrumb): ?><li><?php if (!empty($breadcrumb['url'])): ?><a href="<?= htmlspecialchars($breadcrumb['url']) ?>"><?= htmlspecialchars($breadcrumb['nombre']) ?></a><?php else: ?><?= htmlspecialchars($breadcrumb['nombre']) ?><?php endif; ?></li><?php endforeach; ?></ul><?php endif; ?>
<?php if (!empty($error)): ?><div style="margin-bottom:1rem;padding:0.95rem 1rem;border:1px solid #fecaca;background:#fef2f2;color:#b91c1c;border-radius:12px;"><?= htmlspecialchars($error) ?></div><?php endif; ?>
<div class="page-header"><div><h1 class="page-title">Detalle del Permiso</h1><p class="page-subtitle">Consulta el recurso y las acciones permitidas para este perfil.</p></div></div>
<?php if (!empty($permiso)): ?>
<div class="card"><div class="card-body" style="display:grid;gap:0.7rem;"><div><strong>Perfil:</strong> <?= htmlspecialchars($permiso['str_nombre_perfil'] ?? '') ?></div><div><strong>Módulo:</strong> <?= htmlspecialchars($permiso['str_nombre_modulo'] ?? '') ?></div><div><strong>Submódulo:</strong> <?= htmlspecialchars($permiso['str_nombre_submodulo'] ?? '—') ?></div><div><strong>Ver:</strong> <?= !empty($permiso['bit_consulta']) ? 'Sí' : 'No' ?></div><div><strong>Crear:</strong> <?= !empty($permiso['bit_agregar']) ? 'Sí' : 'No' ?></div><div><strong>Editar:</strong> <?= !empty($permiso['bit_editar']) ? 'Sí' : 'No' ?></div><div><strong>Eliminar:</strong> <?= !empty($permiso['bit_eliminar']) ? 'Sí' : 'No' ?></div><div><strong>Detalle:</strong> <?= !empty($permiso['bit_detalle']) ? 'Sí' : 'No' ?></div></div></div>
<?php endif; ?>
<div style="margin-top:1rem;"><a href="/security/permiso-perfil" class="btn btn-secondary">Volver</a></div>
