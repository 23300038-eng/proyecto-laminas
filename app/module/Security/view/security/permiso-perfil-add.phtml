<div class="page-header">
    <h2>Crear Nuevo Permiso</h2>
</div>

<form method="POST" style="max-width: 600px;">
    <div class="form-row">
        <div class="form-group">
            <label for="id_modulo">Módulo *</label>
            <select id="id_modulo" name="id_modulo" required>
                <option value="">-- Seleccionar --</option>
                <?php foreach ($modulos as $id => $nombre): ?>
                    <option value="<?php echo $id; ?>"><?php echo htmlspecialchars($nombre); ?></option>
                <?php endforeach; ?>
            </select>
        </div>

        <div class="form-group">
            <label for="id_perfil">Perfil *</label>
            <select id="id_perfil" name="id_perfil" required>
                <option value="">-- Seleccionar --</option>
                <?php foreach ($perfiles as $id => $nombre): ?>
                    <option value="<?php echo $id; ?>"><?php echo htmlspecialchars($nombre); ?></option>
                <?php endforeach; ?>
            </select>
        </div>
    </div>

    <fieldset style="border: 1px solid #ddd; padding: 1rem; border-radius: 4px; margin: 1.5rem 0;">
        <legend style="padding: 0 0.5rem;">Permisos</legend>

        <div class="form-check">
            <input type="checkbox" id="bit_agregar" name="bit_agregar" value="1">
            <label for="bit_agregar">Agregar / Crear</label>
        </div>

        <div class="form-check">
            <input type="checkbox" id="bit_editar" name="bit_editar" value="1">
            <label for="bit_editar">Editar / Modificar</label>
        </div>

        <div class="form-check">
            <input type="checkbox" id="bit_consulta" name="bit_consulta" value="1">
            <label for="bit_consulta">Consultar / Ver</label>
        </div>

        <div class="form-check">
            <input type="checkbox" id="bit_eliminar" name="bit_eliminar" value="1">
            <label for="bit_eliminar">Eliminar</label>
        </div>

        <div class="form-check">
            <input type="checkbox" id="bit_detalle" name="bit_detalle" value="1">
            <label for="bit_detalle">Ver Detalle</label>
        </div>
    </fieldset>

    <div class="btn-group" style="margin-top: 2rem;">
        <button type="submit" class="btn btn-primary">Guardar</button>
        <a href="/security/permiso-perfil" class="btn btn-secondary">Cancelar</a>
    </div>
</form>
