<?php if (!empty($breadcrumbs)): ?><ul class="breadcrumbs"><?php foreach ($breadcrumbs as $breadcrumb): ?><li><?php if (!empty($breadcrumb['url'])): ?><a href="<?= htmlspecialchars($breadcrumb['url']) ?>"><?= htmlspecialchars($breadcrumb['nombre']) ?></a><?php else: ?><?= htmlspecialchars($breadcrumb['nombre']) ?><?php endif; ?></li><?php endforeach; ?></ul><?php endif; ?>
<?php if (!empty($error)): ?><div style="margin-bottom:1rem;padding:0.95rem 1rem;border:1px solid #fecaca;background:#fef2f2;color:#b91c1c;border-radius:12px;"><?= htmlspecialchars($error) ?></div><?php endif; ?>
<div class="page-header"><div><h1 class="page-title">Detalle del Perfil</h1><p class="page-subtitle">Resumen de los permisos asignados a este perfil.</p></div></div>
<?php if (!empty($perfil)): ?>
<div class="card" style="margin-bottom:1.25rem;"><div class="card-body" style="display:grid;gap:0.65rem;"><div><strong>Perfil:</strong> <?= htmlspecialchars($perfil['str_nombre_perfil'] ?? '') ?></div><div><strong>Descripción:</strong> <?= htmlspecialchars($perfil['descripcion'] ?? 'Sin descripción') ?></div></div></div>
<div class="card"><div class="card-body"><table class="table"><thead><tr><th>Recurso</th><th>Ver</th><th>Crear</th><th>Editar</th><th>Eliminar</th></tr></thead><tbody>
<?php foreach ($matrix as $modulo): ?>
    <?php $mid=(int)$modulo['id']; $mb=$permisos_indexados['modulos'][$mid] ?? []; ?>
    <tr style="background:#f8fafc;font-weight:700;"><td><?= htmlspecialchars($modulo['str_nombre_modulo']) ?> <span style="color:#64748b;font-weight:400;">(módulo)</span></td><td><?= !empty($mb['consulta']) ? 'Sí' : 'No' ?></td><td><?= !empty($mb['agregar']) ? 'Sí' : 'No' ?></td><td><?= !empty($mb['editar']) ? 'Sí' : 'No' ?></td><td><?= !empty($mb['eliminar']) ? 'Sí' : 'No' ?></td></tr>
    <?php foreach (($modulo['submodulos'] ?? []) as $submodulo): ?>
        <?php $sid=(int)$submodulo['id']; $sb=$permisos_indexados['submodulos'][$sid] ?? []; ?>
        <tr><td style="padding-left:2rem;">↳ <?= htmlspecialchars($submodulo['str_nombre_submodulo']) ?> <span style="color:#64748b;">(submódulo)</span></td><td><?= !empty($sb['consulta']) ? 'Sí' : 'No' ?></td><td><?= !empty($sb['agregar']) ? 'Sí' : 'No' ?></td><td><?= !empty($sb['editar']) ? 'Sí' : 'No' ?></td><td><?= !empty($sb['eliminar']) ? 'Sí' : 'No' ?></td></tr>
    <?php endforeach; ?>
<?php endforeach; ?>
</tbody></table></div></div>
<?php endif; ?>
<div style="margin-top:1rem;"><a href="/security/perfil" class="btn btn-secondary">Volver</a></div>
