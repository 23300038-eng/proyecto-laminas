<?php if (!empty($breadcrumbs)): ?><ul class="breadcrumbs"><?php foreach ($breadcrumbs as $breadcrumb): ?><li><?php if (!empty($breadcrumb['url'])): ?><a href="<?= htmlspecialchars($breadcrumb['url']) ?>"><?= htmlspecialchars($breadcrumb['nombre']) ?></a><?php else: ?><?= htmlspecialchars($breadcrumb['nombre']) ?><?php endif; ?></li><?php endforeach; ?></ul><?php endif; ?>
<div class="page-header"><div><h1 class="page-title">Editar Permiso</h1><p class="page-subtitle">Actualiza las acciones permitidas sobre este recurso.</p></div></div>
<form method="POST" class="card"><div class="card-body" style="max-width:760px;display:grid;gap:1rem;">
<div><strong>Perfil:</strong> <?= htmlspecialchars($permiso['str_nombre_perfil'] ?? '') ?></div>
<div><strong>Módulo:</strong> <?= htmlspecialchars($permiso['str_nombre_modulo'] ?? '') ?></div>
<div><strong>Submódulo:</strong> <?= htmlspecialchars($permiso['str_nombre_submodulo'] ?? '—') ?></div>
<div style="display:flex;gap:1rem;flex-wrap:wrap;"><label><input type="checkbox" name="bit_consulta" value="1" <?= !empty($permiso['bit_consulta']) ? 'checked' : '' ?>> Ver</label><label><input type="checkbox" name="bit_agregar" value="1" <?= !empty($permiso['bit_agregar']) ? 'checked' : '' ?>> Crear</label><label><input type="checkbox" name="bit_editar" value="1" <?= !empty($permiso['bit_editar']) ? 'checked' : '' ?>> Editar</label><label><input type="checkbox" name="bit_eliminar" value="1" <?= !empty($permiso['bit_eliminar']) ? 'checked' : '' ?>> Eliminar</label><label><input type="checkbox" name="bit_detalle" value="1" <?= !empty($permiso['bit_detalle']) ? 'checked' : '' ?>> Detalle</label></div>
<div class="btn-group"><button type="submit" class="btn btn-primary">Actualizar permiso</button><a href="/security/permiso-perfil" class="btn btn-secondary">Cancelar</a></div>
</div></form>
