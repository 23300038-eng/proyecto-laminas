<?php if (!empty($breadcrumbs)): ?>
    <ul class="breadcrumbs">
        <?php foreach ($breadcrumbs as $breadcrumb): ?>
            <li>
                <?php if (!empty($breadcrumb['url'])): ?>
                    <a href="<?= htmlspecialchars($breadcrumb['url']) ?>"><?= htmlspecialchars($breadcrumb['nombre']) ?></a>
                <?php else: ?>
                    <?= htmlspecialchars($breadcrumb['nombre']) ?>
                <?php endif; ?>
            </li>
        <?php endforeach; ?>
    </ul>
<?php endif; ?>

<div class="page-header">
    <div>
        <h1 class="page-title">Inicio</h1>
        <p class="page-subtitle">Resumen general y accesos rápidos de la operación interna de Novafarma.</p>
    </div>
</div>

<div style="display:grid;grid-template-columns:2fr 1fr;gap:1.5rem;align-items:start;">
    <section class="card">
        <div class="card-header">
            <h2 style="margin:0;font-size:1.2rem;">Sobre Novafarma</h2>
        </div>
        <div class="card-body" style="display:grid;gap:1rem;line-height:1.7;">
            <p>
                Novafarma es una farmacéutica enfocada en distribución hospitalaria, control de trazabilidad
                y seguimiento de lotes sensibles. La plataforma interna centraliza la administración de usuarios,
                perfiles y módulos para mantener una operación ordenada, segura y fácil de auditar.
            </p>
            <p>
                Desde esta vista puedes entrar rápidamente a las secciones habilitadas para tu perfil,
                revisar tu información personal y navegar solo por los módulos a los que realmente tienes acceso.
            </p>

            <div style="display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:1rem;">
                <div style="padding:1rem;border:1px solid #e2e8f0;border-radius:14px;background:#f8fafc;">
                    <strong style="display:block;margin-bottom:0.35rem;">Producción segura</strong>
                    <span style="color:#475569;">Control interno alineado a procesos y roles definidos.</span>
                </div>
                <div style="padding:1rem;border:1px solid #e2e8f0;border-radius:14px;background:#f8fafc;">
                    <strong style="display:block;margin-bottom:0.35rem;">Trazabilidad</strong>
                    <span style="color:#475569;">Visibilidad clara de secciones, perfiles y accesos.</span>
                </div>
                <div style="padding:1rem;border:1px solid #e2e8f0;border-radius:14px;background:#f8fafc;">
                    <strong style="display:block;margin-bottom:0.35rem;">Seguridad operativa</strong>
                    <span style="color:#475569;">Permisos aplicados por perfil y acceso segmentado.</span>
                </div>
            </div>
        </div>
    </section>

    <aside class="card">
        <div class="card-header">
            <h2 style="margin:0;font-size:1.2rem;">Acciones rápidas</h2>
        </div>
        <div class="card-body" style="display:grid;gap:0.75rem;">
            <a href="/mi-perfil" class="btn btn-outline">Mi Perfil</a>

            <?php if (!empty($accesos_directos)): ?>
                <?php foreach ($accesos_directos as $modulo): ?>
                    <a href="<?= htmlspecialchars($modulo['url'] ?? '#') ?>" class="btn btn-outline">
                        <?= htmlspecialchars($modulo['nombre'] ?? 'Módulo') ?>
                    </a>
                <?php endforeach; ?>
            <?php else: ?>
                <span style="color:#64748b;">No hay módulos habilitados para tu perfil.</span>
            <?php endif; ?>
        </div>
    </aside>
</div>

<div class="card" style="margin-top:1.5rem;">
    <div class="card-header">
        <h2 style="margin:0;font-size:1.2rem;">Módulos disponibles para tu perfil</h2>
    </div>
    <div class="card-body">
        <?php if (!empty($accesos_directos)): ?>
            <div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:1rem;">
                <?php foreach ($accesos_directos as $modulo): ?>
                    <div style="border:1px solid #e2e8f0;border-radius:16px;padding:1.15rem;background:#fff;">
                        <h3 style="margin:0 0 0.5rem;font-size:1.05rem;"><?= htmlspecialchars($modulo['nombre']) ?></h3>
                        <p style="margin:0 0 1rem;color:#64748b;line-height:1.6;">
                            Accede a las opciones habilitadas de este módulo según los permisos asignados a tu perfil.
                        </p>
                        <a href="<?= htmlspecialchars($modulo['url'] ?? '#') ?>" class="btn btn-primary">Abrir módulo</a>

                        <?php if (!empty($modulo['items'])): ?>
                            <div style="margin-top:1rem;padding-top:1rem;border-top:1px solid #e2e8f0;">
                                <strong style="display:block;margin-bottom:0.6rem;">Secciones disponibles</strong>
                                <ul style="margin:0;padding-left:1rem;display:grid;gap:0.45rem;">
                                    <?php foreach ($modulo['items'] as $item): ?>
                                        <li>
                                            <a href="<?= htmlspecialchars($item['url']) ?>"><?= htmlspecialchars($item['nombre']) ?></a>
                                        </li>
                                    <?php endforeach; ?>
                                </ul>
                            </div>
                        <?php endif; ?>
                    </div>
                <?php endforeach; ?>
            </div>
        <?php else: ?>
            <p style="margin:0;color:#64748b;">Actualmente no tienes módulos visibles asignados.</p>
        <?php endif; ?>
    </div>
</div>
