<?php if (!empty($breadcrumbs)): ?>
    <ul class="breadcrumbs">
        <?php foreach ($breadcrumbs as $breadcrumb): ?>
            <li>
                <?php if (!empty($breadcrumb['url'])): ?>
                    <a href="<?= htmlspecialchars($breadcrumb['url']) ?>"><?= htmlspecialchars($breadcrumb['nombre']) ?></a>
                <?php else: ?>
                    <?= htmlspecialchars($breadcrumb['nombre']) ?>
                <?php endif; ?>
            </li>
        <?php endforeach; ?>
    </ul>
<?php endif; ?>

<?php if (!empty($estadisticas)): ?>
    <div class="page-header">
        <div>
            <h1 class="page-title">Panel de Control</h1>
            <p class="page-subtitle">Resumen administrativo con datos reales del sistema.</p>
        </div>
    </div>

    <div style="display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:1rem;margin-bottom:1.5rem;">
        <div class="card"><div class="card-body"><strong style="display:block;font-size:2rem;"><?= (int) $estadisticas['usuarios'] ?></strong><span>Usuarios registrados</span></div></div>
        <div class="card"><div class="card-body"><strong style="display:block;font-size:2rem;"><?= (int) $estadisticas['modulos'] ?></strong><span>Módulos existentes</span></div></div>
        <div class="card"><div class="card-body"><strong style="display:block;font-size:2rem;"><?= (int) $estadisticas['submodulos'] ?></strong><span>Submódulos existentes</span></div></div>
        <div class="card"><div class="card-body"><strong style="display:block;font-size:2rem;"><?= (int) $estadisticas['perfiles'] ?></strong><span>Perfiles registrados</span></div></div>
    </div>

    <div style="display:grid;grid-template-columns:1.4fr 1fr;gap:1.5rem;">
        <section class="card">
            <div class="card-header">
                <h2 style="margin:0;font-size:1.15rem;">Actividad reciente de usuarios</h2>
            </div>
            <div class="card-body">
                <?php if (!empty($usuarios_recientes)): ?>
                    <table class="table">
                        <thead>
                            <tr>
                                <th>Usuario</th>
                                <th>Correo</th>
                                <th>Perfil</th>
                                <th>Estado</th>
                            </tr>
                        </thead>
                        <tbody>
                            <?php foreach ($usuarios_recientes as $usuario): ?>
                                <tr>
                                    <td><?= htmlspecialchars($usuario['str_nombre_usuario']) ?></td>
                                    <td><?= htmlspecialchars($usuario['str_correo']) ?></td>
                                    <td><?= htmlspecialchars($usuario['str_nombre_perfil']) ?></td>
                                    <td><?= htmlspecialchars($usuario['str_nombre_estado'] ?? '') ?></td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>
                    </table>
                <?php else: ?>
                    <p style="margin:0;color:#64748b;">No hay usuarios recientes para mostrar.</p>
                <?php endif; ?>
            </div>
        </section>

        <aside class="card">
            <div class="card-header">
                <h2 style="margin:0;font-size:1.15rem;">Accesos administrativos</h2>
            </div>
            <div class="card-body" style="display:grid;gap:0.75rem;">
                <a href="/security/usuario" class="btn btn-outline">Gestionar usuarios</a>
                <a href="/security/perfil" class="btn btn-outline">Gestionar perfiles</a>
                <a href="/security/modulo" class="btn btn-outline">Gestionar módulos</a>
                <a href="/security/permiso-perfil" class="btn btn-outline">Gestionar permisos</a>
            </div>
        </aside>
    </div>
<?php else: ?>
    <div class="page-header">
        <div>
            <h1 class="page-title"><?= htmlspecialchars($titulo ?? 'Módulo') ?></h1>
            <p class="page-subtitle"><?= htmlspecialchars($descripcion ?? '') ?></p>
        </div>
    </div>

    <div class="card">
        <div class="card-body" style="display:grid;gap:1rem;">
            <p style="margin:0;line-height:1.7;"><?= htmlspecialchars($descripcion ?? '') ?></p>

            <div style="display:flex;gap:0.75rem;flex-wrap:wrap;">
                <button class="btn btn-primary" type="button">Crear</button>
                <button class="btn btn-secondary" type="button">Consultar</button>
                <button class="btn btn-secondary" type="button">Editar</button>
                <button class="btn btn-danger" type="button">Eliminar</button>
            </div>

            <div style="padding:1rem;border-radius:14px;background:#eff6ff;border:1px solid #bfdbfe;color:#1e3a8a;">
                Esta vista es demostrativa. El acceso al módulo ya se encuentra controlado por los permisos del perfil del usuario actual.
            </div>
        </div>
    </div>
<?php endif; ?>
