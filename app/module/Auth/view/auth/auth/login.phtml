<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Novafarma - Acceso al Sistema</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html, body { height: 100%; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            background: #f0f4f8;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 20px;
        }
        .login-wrapper { width: 100%; max-width: 420px; }
        .login-card {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 24px rgba(0,0,0,0.10);
            overflow: hidden;
        }
        .login-header {
            background: #0055b3;
            color: white;
            padding: 2.5rem 2rem;
            text-align: center;
        }
        .login-header h1 { font-size: 2rem; font-weight: 700; margin-bottom: 4px; letter-spacing: 1px; }
        .login-header p { font-size: 0.88rem; opacity: 0.85; font-weight: 300; }
        .login-content { padding: 2rem; }
        .form-group { margin-bottom: 1.25rem; }
        .form-group label {
            display: block;
            margin-bottom: 6px;
            font-weight: 500;
            color: #374151;
            font-size: 0.9rem;
        }
        .form-group input[type="text"],
        .form-group input[type="password"],
        .form-group input[type="number"] {
            width: 100%;
            padding: 0.75rem 1rem;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            font-size: 0.95rem;
            background: #f9fafb;
            transition: border-color 0.2s, box-shadow 0.2s;
        }
        .form-group input:focus {
            outline: none;
            border-color: #0055b3;
            background: #fff;
            box-shadow: 0 0 0 3px rgba(0,85,179,0.10);
        }
        .captcha-row {
            display: flex;
            align-items: center;
            gap: 12px;
        }
        .captcha-question {
            background: #f0f4f8;
            border: 1px solid #d1d5db;
            border-radius: 6px;
            padding: 0.75rem 1rem;
            font-size: 1rem;
            font-weight: 600;
            color: #0055b3;
            white-space: nowrap;
            min-width: 110px;
            text-align: center;
        }
        .captcha-row input[type="number"] {
            flex: 1;
            max-width: 100px;
        }
        .alert {
            padding: 0.85rem 1rem;
            margin-bottom: 1.25rem;
            border-radius: 6px;
            background: #fef2f2;
            color: #b91c1c;
            border-left: 4px solid #dc2626;
            font-size: 0.88rem;
        }
        .btn-login {
            width: 100%;
            padding: 0.85rem;
            background: #0055b3;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            margin-top: 0.5rem;
            transition: background 0.2s, transform 0.1s;
            letter-spacing: 0.3px;
        }
        .btn-login:hover { background: #003d85; transform: translateY(-1px); }
        .btn-login:active { transform: none; }
        .login-footer {
            text-align: center;
            padding: 1.25rem 2rem;
            background: #f9fafb;
            border-top: 1px solid #e5e7eb;
            color: #9ca3af;
            font-size: 0.8rem;
        }
        @media (max-width: 480px) {
            .login-header { padding: 2rem 1.5rem; }
            .login-content { padding: 1.5rem; }
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <div class="login-card">
            <div class="login-header">
                <h1>Novafarma</h1>
                <p>Sistema de Gestión Administrativa</p>
            </div>
            <div class="login-content">
                <?php if (!empty($error)): ?>
                    <div class="alert"><?= htmlspecialchars($error) ?></div>
                <?php endif; ?>

                <form method="POST" action="" autocomplete="off">
                    <div class="form-group">
                        <label for="username">Usuario</label>
                        <input type="text" id="username" name="username" required autofocus
                               placeholder="Ingrese su usuario">
                    </div>

                    <div class="form-group">
                        <label for="password">Contraseña</label>
                        <input type="password" id="password" name="password" required
                               placeholder="Ingrese su contraseña">
                    </div>

                    <div class="form-group">
                        <label>Verificación CAPTCHA</label>
                        <div class="captcha-row">
                            <div class="captcha-question">
                                <?= (int)$captcha_n1 ?> + <?= (int)$captcha_n2 ?> = ?
                            </div>
                            <input type="number" name="captcha" id="captcha" required
                                   placeholder="Resultado" min="0" max="18">
                        </div>
                    </div>

                    <button type="submit" class="btn-login">Iniciar Sesión</button>
                </form>
            </div>
            <div class="login-footer">
                &copy; <?= date('Y') ?> Novafarma &mdash; Todos los derechos reservados.
            </div>
        </div>
    </div>
</body>
</html>
