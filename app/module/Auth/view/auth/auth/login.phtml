<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Novafarma - Acceso al Sistema</title>
    <script src="https://js.hcaptcha.com/1/api.js?hl=es" async defer></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        html, body { min-height: 100%; }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial, sans-serif;
            background: linear-gradient(135deg, #eff6ff 0%, #dbeafe 40%, #f8fafc 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            padding: 24px;
            color: #0f172a;
        }
        .login-wrapper {
            width: 100%;
            max-width: 430px;
        }
        .login-card {
            background: #ffffff;
            border-radius: 18px;
            box-shadow: 0 18px 45px rgba(15, 23, 42, 0.14);
            overflow: hidden;
            border: 1px solid rgba(148, 163, 184, 0.18);
        }
        .login-header {
            padding: 2.2rem 2rem 1.2rem;
            text-align: center;
        }
        .login-badge {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 62px;
            height: 62px;
            border-radius: 16px;
            background: #dbeafe;
            color: #1d4ed8;
            font-size: 1.8rem;
            margin-bottom: 1rem;
        }
        .login-header h1 {
            font-size: 2rem;
            font-weight: 800;
            color: #0f172a;
            margin-bottom: 0.35rem;
        }
        .login-header p {
            color: #475569;
            font-size: 0.96rem;
            line-height: 1.5;
        }
        .login-content {
            padding: 0 2rem 2rem;
        }
        .form-group {
            margin-bottom: 1.15rem;
        }
        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 600;
            color: #334155;
            font-size: 0.92rem;
        }
        .form-group input[type="email"],
        .form-group input[type="password"] {
            width: 100%;
            padding: 0.9rem 1rem;
            border: 1px solid #cbd5e1;
            border-radius: 12px;
            font-size: 0.96rem;
            background: #f8fafc;
            transition: border-color 0.2s, box-shadow 0.2s, background-color 0.2s;
        }
        .form-group input:focus {
            outline: none;
            background: #ffffff;
            border-color: #2563eb;
            box-shadow: 0 0 0 4px rgba(37, 99, 235, 0.12);
        }
        .alert {
            padding: 0.95rem 1rem;
            margin-bottom: 1.2rem;
            border-radius: 12px;
            background: #fef2f2;
            color: #b91c1c;
            border: 1px solid #fecaca;
            font-size: 0.92rem;
            line-height: 1.45;
        }
        .captcha-box {
            margin: 1.35rem 0 1.1rem;
            display: flex;
            justify-content: center;
        }
        .btn-login {
            width: 100%;
            padding: 0.95rem 1rem;
            margin-top: 0.25rem;
            background: linear-gradient(135deg, #2563eb 0%, #1d4ed8 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 700;
            cursor: pointer;
            transition: transform 0.1s ease, box-shadow 0.2s ease, opacity 0.2s ease;
        }
        .btn-login:hover {
            box-shadow: 0 12px 24px rgba(37, 99, 235, 0.18);
            transform: translateY(-1px);
        }
        .btn-login:active {
            transform: translateY(0);
        }
        .login-footer {
            text-align: center;
            padding-top: 1rem;
            color: #64748b;
            font-size: 0.84rem;
        }
        @media (max-width: 480px) {
            .login-header {
                padding: 2rem 1.5rem 1rem;
            }
            .login-content {
                padding: 0 1.5rem 1.75rem;
            }
        }
    </style>
</head>
<body>
    <div class="login-wrapper">
        <div class="login-card">
            <div class="login-header">
                <div class="login-badge">💊</div>
                <h1>Novafarma</h1>
                <p>Ingresa a la plataforma administrativa para gestionar usuarios, perfiles y módulos del sistema.</p>
            </div>

            <div class="login-content">
                <?php if (!empty($error)): ?>
                    <div class="alert"><?= htmlspecialchars($error) ?></div>
                <?php endif; ?>

                <form method="POST" action="" autocomplete="off">
                    <div class="form-group">
                        <label for="email">Correo electrónico</label>
                        <input
                            type="email"
                            id="email"
                            name="email"
                            required
                            autofocus
                            placeholder="Ingresa tu correo electrónico" autocomplete="username"
                        >
                    </div>

                    <div class="form-group">
                        <label for="password">Contraseña</label>
                        <input
                            type="password"
                            id="password"
                            name="password"
                            required
                            placeholder="Ingresa tu contraseña" autocomplete="current-password" minlength="8"
                        >
                    </div>

                    <div class="captcha-box">
                        <div class="h-captcha" data-sitekey="<?= htmlspecialchars($siteKey ?? '') ?>"></div>
                    </div>

                    <button type="submit" class="btn-login">Iniciar sesión</button>
                </form>

                <div class="login-footer">
                    &copy; <?= date('Y') ?> Novafarma. Plataforma interna de gestión farmacéutica.
                </div>
            </div>
        </div>
    </div>
</body>
</html>
